# Versions validated for serving on CUDA 12.8. Install these only on the vLLM
# server. VLMEvalKit client dependencies are installed by scripts/bootstrap.sh.
torch==2.9.0
transformers==4.57.6
vllm==0.11.2
qwen-vl-utils==0.0.14
